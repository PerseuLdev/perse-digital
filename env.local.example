# =============================================================================
# Stripe — copiar para .env.local e preencher com seus valores
# =============================================================================

# Chaves da API (Dashboard > Developers > API keys)
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Webhook secret (obtido via `stripe listen` em dev ou no Dashboard em prod)
STRIPE_WEBHOOK_SECRET=whsec_...

# =============================================================================
# App
# =============================================================================

NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Resend (Dashboard > API Keys)
RESEND_API_KEY=re_...
NOTIFICATION_EMAIL=seuemail@persedigital.com.br

# =============================================================================
# Price IDs — Criar no Stripe Dashboard > Products (modo Test primeiro)
# =============================================================================
#
# BRL — dois produtos por tier (PIX com desconto e Cartão com parcelamento)
#
# Plano Essential BRL
STRIPE_PRICE_ESSENTIAL_PIX_BRL=price_...   # R$ 699,00 — PIX à vista (one-time)
STRIPE_PRICE_ESSENTIAL_CARD_BRL=price_...  # R$ 718,80 — 12x R$ 59,90 (one-time)

# Plano Professional BRL
STRIPE_PRICE_PROFESSIONAL_PIX_BRL=price_...   # R$ 1.799,00 — PIX à vista (one-time)
STRIPE_PRICE_PROFESSIONAL_CARD_BRL=price_...  # R$ 2.158,80 — 12x R$ 179,90 (one-time)

#
# USD — preço único por tier, sem parcelamento
#
STRIPE_PRICE_ESSENTIAL_USD=price_...      # $497.00 — one-time
STRIPE_PRICE_PROFESSIONAL_USD=price_...   # $997.00 — one-time

# =============================================================================
# Notas
# =============================================================================
# - PIX e Boleto habilitados automaticamente no Dashboard para currency=BRL
# - Parcelamento em até 12x no cartão BRL: ativado via installments.enabled=true
#   no checkout session (configurado no código)
# - Para go-live: trocar sk_test_ → sk_live_ e criar novo webhook endpoint
# - Eventos necessários no Dashboard:
#     checkout.session.completed
#     checkout.session.async_payment_succeeded
#     checkout.session.async_payment_failed
