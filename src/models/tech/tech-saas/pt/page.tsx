import { ModelPlaceholder } from '@/components/placeholders/model-placeholder';
import { config } from '../config';

export default function TechSaasPT() {
  return (
    <ModelPlaceholder
      modelName="Tech SaaS"
      colors={config.colors}
      locale="pt"
    />
  );
}
